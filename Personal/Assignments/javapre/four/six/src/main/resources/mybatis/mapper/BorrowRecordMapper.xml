<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lab.borrow.mapper.BorrowRecordMapper">

    <select id="countActiveByUserId" resultType="long">
        SELECT COUNT(1) FROM borrow_record WHERE user_id = #{userId} AND status = 'borrowed'
    </select>

    <select id="countAnyByUserId" resultType="long">
        SELECT COUNT(1) FROM borrow_record WHERE user_id = #{userId}
    </select>

    <select id="countActiveByDeviceId" resultType="long">
        SELECT COUNT(1) FROM borrow_record WHERE device_id = #{deviceId} AND status = 'borrowed'
    </select>

</mapper>
