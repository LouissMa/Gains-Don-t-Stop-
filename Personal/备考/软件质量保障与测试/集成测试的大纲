下面是《第5章 集成测试》的期末复习详细总结，按 PPT 原有结构整理，重点已加粗。

# 第5章 集成测试

## 0. 学习目标

本章主要解决四个问题：

**1. 什么是集成测试**
**2. 集成测试和单元测试有什么区别**
**3. 集成测试有哪些策略**
**4. 集成测试用例如何设计，过程如何开展**

一句话理解：

> 单元测试检查“一个模块自己对不对”，集成测试检查“多个模块连在一起后能不能正确协作”。

---

# 1. 基本概念

## 1.1 集成测试定义

**集成测试是在单元测试的基础上，将多个模块组合在一起进行测试的过程，主要检查各个软件单元之间的相互接口是否正确。**

它处在：

```text
单元测试 → 集成测试 → 系统测试
```

之间。

也就是说：

* 单元测试：测单个模块
* 集成测试：测模块之间的连接
* 系统测试：测整个系统是否满足用户需求

## 1.2 集成测试的定位

PPT 强调：

**集成测试是单元测试的扩展和延伸，是介于单元测试和系统测试之间的过渡阶段。**

但是注意：

**集成测试不能确认软件在实际运行环境下是否完全满足用户需求。**

因为集成测试主要关注模块接口和组合功能，不等于完整用户场景测试。因此软件完成后还必须进行系统测试。

## 1.3 重要原则

PPT 特别强调：

**没有经过单元测试的模块，不应该直接进行集成测试。**

原因是：

如果单个模块本身就有问题，那么集成时出现错误后，很难判断问题到底来自：

* 模块内部逻辑错误
* 模块接口错误
* 模块组合错误
* 数据传递错误

所以逻辑链是：

```text
单元正确 → 才能集成
接口正确 → 才能组合
组合正确 → 才能系统测试
```

---

## 1.4 集成测试主要任务

集成测试的核心任务是：

**检验软件系统是否符合实际软件结构，发现与接口有关的各种错误。**

具体包括 5 个方面：

### 1. 模块连接时数据是否丢失

例如 A 模块调用 B 模块，把 userId 传过去，但 B 接收到的是 null 或错误值。

这是典型的接口数据传递问题。

### 2. 子功能组合后能否达到预期功能

单个模块都正确，不代表组合起来正确。

例如：

* 登录模块正常
* 购物车模块正常
* 支付模块正常

但组合成“登录 → 加购 → 支付”流程时可能失败。

### 3. 一个模块是否会影响其他模块

例如修改订单模块后，导致支付模块异常。

这类问题属于模块间副作用。

### 4. 全局数据结构是否有问题

例如多个模块共享同一个全局变量，其中一个模块异常修改，导致其他模块错误。

### 5. 单个模块误差累积是否被放大

例如每个模块都有一点数值误差，单独看可以接受，但多个模块组合后误差越来越大，最终不可接受。

---

## 1.5 单元测试与集成测试区别

| 对比项    | 单元测试      | 集成测试           |
| ------ | --------- | -------------- |
| 测试对象   | 单个函数、类、模块 | 多个模块及其组合       |
| 对应设计阶段 | 详细设计      | 概要设计           |
| 测试时间   | 先进行       | 后进行            |
| 测试重点   | 模块内部逻辑    | 模块接口、数据传递、组合功能 |
| 常用方法   | 白盒为主      | 黑盒为主，结合接口测试    |
| 目标     | 保证单个模块正确  | 保证模块协作正确       |

**高频考点：**

> 单元测试主要测试模块内部逻辑，集成测试主要测试接口、接口之间的数据传递关系，以及模块组合后能否达到预期效果。

---

## 本节可能出题

### 题1：什么是集成测试？

答：集成测试是在单元测试基础上，将多个模块组合在一起进行测试，主要检查软件单元之间的接口是否正确。

### 题2：为什么未经过单元测试的模块不应进行集成测试？

答：因为模块自身缺陷会干扰集成测试结果，使错误定位困难，降低集成测试效率和效果。

### 题3：集成测试主要检查哪些问题？

答：模块接口数据是否丢失、子功能组合是否达到预期、模块间是否产生不良影响、全局数据结构是否异常、误差是否累积放大。

---

# 2. 集成测试的层次和原则

## 2.1 集成测试层次

### 传统软件的集成测试层次

按照集成粒度不同，分为 3 个层次：

1. **模块内集成测试**
2. **子系统内集成测试，也叫模块间集成测试**
3. **子系统间集成测试**

逻辑是从小到大：

```text
模块内部组合 → 模块之间组合 → 子系统之间组合
```

### 面向对象系统的集成测试层次

面向对象软件中，主要分为：

1. **类内集成测试**
2. **类间集成测试**

也就是：

* 类内：检查类内部方法之间协作
* 类间：检查不同类之间调用关系

---

## 2.2 集成测试原则

PPT 中列出了 8 条原则，期末很容易考简答题。

### 1. **所有公共接口都要被测试到**

接口是集成测试的核心对象，只要是模块之间公开交互的接口，就必须覆盖。

### 2. **关键模块必须进行充分测试**

关键模块一般指：

* 主控模块
* 核心业务模块
* 高风险模块
* 被多个模块依赖的模块

### 3. **集成测试应按一定层次进行**

不能随意拼接模块，应按照软件结构和依赖关系逐步集成。

### 4. **策略选择要综合考虑质量、成本和进度**

不同策略没有绝对最好，要看项目情况。

比如：

* 项目小：可以考虑大爆炸集成
* 项目复杂：更适合渐增式集成
* 核心功能重要：可用核心系统先行集成

### 5. **集成测试应尽早开始，并以总体设计为基础**

因为集成测试主要对应概要设计，不是等所有代码都完成才开始考虑。

### 6. **测试人员应与开发人员充分沟通接口划分**

接口理解错误，是集成测试常见错误来源。

### 7. **接口修改后，相关接口必须再测试**

这就是回归测试思想。

### 8. **测试结果应如实记录**

测试报告必须真实，否则无法定位问题和评估质量。

---

## 本节可能出题

### 题1：传统软件集成测试分为哪三个层次？

答：模块内集成测试、子系统内集成测试、子系统间集成测试。

### 题2：面向对象系统集成测试分为哪两个层次？

答：类内集成测试和类间集成测试。

### 题3：简述集成测试原则。

答：公共接口必须测试、关键模块充分测试、按层次集成、平衡质量成本进度、尽早开始、测试与开发沟通接口、接口修改后再测试、如实记录结果。

---

# 3. 集成测试策略

这是本章最重要部分，期末最容易出选择题、简答题和比较题。

## 3.1 集成策略概述

**集成策略决定各个模块被合并进来的顺序。**

集成策略大体分为：

1. **非渐增式集成**
2. **渐增式集成**
3. 其他集成策略

---

## 3.2 驱动模块与桩模块

这是高频名词解释。

### 驱动模块 Driver

**驱动模块用于模拟待测模块的上级模块。**

作用：

* 接收测试数据
* 将数据传给被测模块
* 启动被测模块
* 打印或记录测试结果

通俗理解：

> 驱动模块是假装“调用你的人”。

### 桩模块 Stub

**桩模块用于模拟待测模块工作过程中所调用的下级模块。**

作用：

* 被待测模块调用
* 模拟下级模块返回结果
* 帮助检查上层模块与下层模块接口

通俗理解：

> 桩模块是假装“被你调用的人”。

### 记忆方式

```text
Driver → 被测模块 → Stub
上级模块 → 当前模块 → 下级模块
```

---

## 3.3 非渐增式集成：大爆炸集成

### 定义

**非渐增式集成也称大爆炸式集成，即在每个单元都完成单元测试后，把所有模块一次性集成到一起进行测试。**

英文：Big-Bang Integration。

### 特点

一次性把所有模块组合起来，不考虑模块间依赖和风险。

### 优点

1. 可以并行测试所有模块
2. 需要的测试用例较少
3. 方法简单、容易实施

### 缺点

这是考试重点。

1. **一次运行成功的可能性不大**

因为模块接口、全局数据结构等问题会集中暴露。

2. **错误定位困难**

大量模块一起集成，出现错误时很难判断是哪个模块或接口导致的。

3. **新旧错误混杂**

修改一个错误可能引入新的错误，导致调试复杂。

4. **即使通过，也可能遗漏很多错误**

因为没有逐步验证接口。

### 适用范围

适用于：

1. 只修改或增加少数模块的稳定项目
2. 功能少、模块少、逻辑简单的小型项目
3. 单元测试质量非常高的软件项目

### 一句话总结

> 大爆炸集成简单快速，但风险高、定位难，只适合小型或高度稳定项目。

---

## 3.4 渐增式集成

渐增式集成是逐步把模块加入系统，每加入一个模块就测试一次。

主要包括：

1. 自顶向下集成
2. 自底向上集成

---

# 3.4.1 自顶向下集成

## 定义

**自顶向下集成是从主控模块开始，按照软件控制层次结构，逐步把下层模块集成进来。**

英文：Top-down Integration。

逻辑：

```text
顶层主控模块 → 中间模块 → 底层模块
```

## 过程

1. 先测试主控模块
2. 主控模块调用的下层模块先用桩模块代替
3. 每次用一个真实模块替换一个桩
4. 每加入一个新模块就测试
5. 做必要的回归测试
6. 重复直到所有模块加入

## 两种顺序

### 1. 深度优先

沿着一条调用链一直向下集成。

例如：

```text
A → B → E
```

然后再测其他分支。

### 2. 广度优先

先集成同一层所有模块，再进入下一层。

例如：

```text
先 A
再 B、C、D
再 E、F、G
```

## 优点

1. **能较早验证主要控制模块**

因为顶层模块通常负责系统主要控制逻辑。

2. **能较早发现高层设计问题**

如果主控流程有问题，可以早发现。

3. 可以较早看到系统框架

这对管理和演示有帮助。

## 缺点

1. **需要开发大量桩模块**

因为下层模块尚未集成，需要 Stub 模拟。

2. 底层模块测试可能推迟

底层细节问题不一定早暴露。

3. 若软件调用关系和设计结构不一致，测试会复杂

## 一句话总结

> 自顶向下集成适合早期验证主控流程，但需要大量桩模块。

---

# 3.4.2 自底向上集成

## 定义

**自底向上集成是从软件结构的底层模块开始，逐步向上集成，最终形成完整系统。**

英文：Bottom-up Integration。

逻辑：

```text
底层模块 → 中间模块 → 顶层模块
```

## 过程

1. 从底层模块开始测试
2. 将两个或多个底层模块组合成模块组
3. 开发驱动模块调用底层模块
4. 用真实上层模块替换驱动模块
5. 重复直到顶层模块加入

## 优点

1. **可以较早验证底层模块行为**

底层模块通常是基础服务、工具类、数据处理模块。

2. 初期可以并行集成和测试

多个底层模块可以同时测试。

3. 不需要桩模块

因为从底层开始，通常没有更低层依赖。

4. 额外工作量相对较少

主要是不需要维护大量 Stub。

## 缺点

1. **需要较多驱动模块**

因为上层模块还没集成，需要 Driver 调用底层模块。

2. **高层验证推迟**

如果高层设计有问题，发现较晚。

3. 可能导致返工甚至重构

如果最后才发现主控逻辑错误，影响很大。

## 一句话总结

> 自底向上集成适合早期验证底层模块，但高层控制问题发现较晚。

---

## 3.5 自顶向下 VS 自底向上

| 对比项    | 自顶向下       | 自底向上          |
| ------ | ---------- | ------------- |
| 集成方向   | 从顶层到低层     | 从底层到高层        |
| 需要辅助模块 | 需要桩模块 Stub | 需要驱动模块 Driver |
| 优点     | 早验证主控逻辑    | 早验证底层功能       |
| 缺点     | 桩模块多       | 驱动模块多         |
| 风险     | 底层问题发现较晚   | 高层问题发现较晚      |
| 适合场景   | 控制逻辑重要     | 底层功能复杂或关键     |

考试常问：

> 自顶向下不需要驱动模块，但需要桩模块。
> 自底向上不需要桩模块，但需要驱动模块。

---

## 3.6 三明治集成

### 定义

**三明治集成是一种混合增量式集成策略，综合自顶向下和自底向上两种方法。**

它将系统划分为三层：

```text
顶层
目标层
底层
```

然后：

* 目标层以上：采用自顶向下集成
* 目标层以下：采用自底向上集成

### 优点

1. 结合两种策略优点
2. 能同时验证高层控制和底层基础功能
3. 有助于尽早发现缺陷
4. 提高工作效率

### 缺点

1. 管理复杂度较高
2. 需要合理选择目标层
3. 可能同时需要 Driver 和 Stub

### 一句话总结

> 三明治集成是自顶向下和自底向上的结合，适合中大型系统。

---

## 3.7 核心系统先行集成

### 定义

**先对核心软件部分进行集成测试，测试通过后，再按外围软件的重要程度逐个集成到核心系统。**

### 适用场景

适合：

* 核心功能非常重要的系统
* 快速开发项目
* 需要优先保证主业务流程可用的系统

例如电商系统：

先集成：

```text
用户登录 → 商品 → 订单 → 支付
```

再逐步加入：

```text
优惠券、推荐、评论、客服、物流追踪
```

### 优点

可以优先保证重要功能和服务实现。

---

## 3.8 持续集成 CI

### 定义

**持续集成是指开发过程中各个模块不是等全部完成后才集成，而是根据进度尽早、频繁地进行集成。**

核心目标：

* 尽早发现 Bug
* 避免最后集中爆发大量 Bug
* 更容易定位和修复问题
* 提高质量和效率

### CI 需要的基础

PPT 中提到：

1. CI 文化
2. CI 流程
3. 良好基础设施
4. 统一代码库
5. 定期提交代码
6. 自动、快速构建
7. 自动部署
8. 自动集成测试

### 一句话总结

> 持续集成强调“频繁提交、自动构建、自动测试、快速反馈”。

---

## 3.9 策略比较总结

| 策略     | 核心特点      | 优点      | 缺点        |
| ------ | --------- | ------- | --------- |
| 大爆炸集成  | 一次性集成全部模块 | 简单、用例少  | 风险高、定位难   |
| 自顶向下   | 从主控模块向下   | 早验证主流程  | 需要大量桩     |
| 自底向上   | 从底层模块向上   | 早验证底层   | 需要驱动，高层晚测 |
| 三明治集成  | 上下结合      | 综合优势    | 组织复杂      |
| 核心系统先行 | 先集成核心功能   | 保障关键功能  | 需识别核心模块   |
| 持续集成   | 频繁自动集成    | 早发现、易定位 | 依赖工具和流程   |

---

## 本节可能出题

### 题1：什么是大爆炸集成？有什么优缺点？

答：大爆炸集成是把所有通过单元测试的模块一次性集成。优点是简单、测试用例少、可并行；缺点是成功率低、错误定位困难、可能遗漏错误。

### 题2：比较自顶向下和自底向上集成。

答：自顶向下从主控模块开始，需要桩模块，能早验证主控逻辑；自底向上从底层模块开始，需要驱动模块，能早验证底层功能。

### 题3：什么是持续集成？

答：持续集成是开发过程中将完成的模块尽早、频繁地集成，并通过自动构建、自动部署、自动测试快速发现并修复问题。

---

# 4. 集成测试用例设计

PPT 将集成测试用例设计分为 6 类。

## 4.1 为系统运行设计用例

这是最基础的用例。

目的：

**先让系统能够运行起来。**

这类用例应覆盖程序最主要的执行路径。

逻辑是：

```text
先保证能跑 → 再保证跑得对 → 再测试异常和特殊情况
```

例如：

登录系统最基本用例：

```text
输入正确用户名和密码 → 登录成功 → 进入首页
```

如果这个都不通过，后续测试都会被阻塞。

---

## 4.2 为正向测试设计用例

### 定义

**正向测试用例用于验证需求和设计是否得到满足，软件功能是否实现。**

它关注：

* 正确输入
* 正常流程
* 预期功能

例如：

电商系统中：

```text
正常登录 → 正常选购 → 正常支付 → 订单生成
```

### 考点

正向测试主要根据：

**需求规格说明书和概要设计** 来设计。

---

## 4.3 为逆向测试设计用例

### 定义

**逆向测试主要以已知缺陷空间为依据设计用例，用于证明已知缺陷在软件中不存在。**

通俗理解：

> 我知道这里容易出错，所以专门设计用例去攻击它。

例如：

* 输入非法参数
* 模块接口传空值
* 传递超长字符串
* 调用顺序错误
* 网络异常
* 数据格式不匹配

### 与正向测试区别

| 类型   | 目标         |
| ---- | ---------- |
| 正向测试 | 证明功能可以正常工作 |
| 逆向测试 | 证明已知缺陷不会发生 |

---

## 4.4 为特殊需求设计用例

除了功能需求，还需要测试：

1. 安全性需求
2. 性能需求
3. 可靠性需求
4. 内存越界
5. 异常处理
6. 并发情况

例如：

支付系统不只要支付成功，还要测试：

* 是否防止重复支付
* 并发支付是否正确
* 支付接口超时是否处理
* 金额是否被篡改
* 性能是否满足高并发

---

## 4.5 为高覆盖率设计用例

这里非常重要。

**单元测试覆盖率主要在代码层面。**

例如：

* 语句覆盖
* 判定覆盖
* 条件覆盖
* 路径覆盖

**集成测试覆盖率主要在接口层面。**

例如：

* 接口覆盖率
* 接口路径覆盖率

也就是说：

```text
单元测试：关注代码是否被执行
集成测试：关注接口是否被调用、接口路径是否被覆盖
```

---

## 4.6 基于模块接口依赖关系设计用例

模块接口依赖关系图通常是：

**有向无环图 DAG。**

如果：

```text
A 调用 B 和 C
```

那么集成测试不只要测：

```text
A → B
A → C
```

还要考虑：

```text
A 同时组合调用 B 和 C
```

因为 B 和 C 的组合可能产生新的接口问题。

---

## 本节可能出题

### 题1：集成测试用例设计包括哪些类型？

答：为系统运行设计用例、为正向测试设计用例、为逆向测试设计用例、为特殊需求设计用例、为高覆盖率设计用例、基于模块接口依赖关系设计用例。

### 题2：正向测试和逆向测试区别？

答：正向测试验证需求和功能是否实现；逆向测试依据已知缺陷空间设计，用于证明缺陷不存在。

### 题3：集成测试覆盖率和单元测试覆盖率有什么区别？

答：单元测试覆盖率主要在代码层面，集成测试覆盖率主要在接口层面，如接口覆盖率和接口路径覆盖率。

---

# 5. 集成测试过程

PPT 将集成测试过程划分为 3 个阶段：

1. 计划阶段
2. 设计实现阶段
3. 执行评估阶段

---

## 5.1 计划阶段

### 开始时间

**概要设计完成评审之后，进入集成测试计划阶段。**

这说明集成测试不是编码完成后才考虑，而是概要设计后就应规划。

### 主要工作

1. 准备需求规格说明书
2. 准备概要设计文档
3. 确定测试范围
4. 确定测试对象
5. 准备测试工具和环境
6. 组织测试人员
7. 考虑风险和应急计划
8. 准备外部技术支援和培训

### 阶段输出

**集成测试计划书，并通过评审。**

---

## 5.2 设计实现阶段

这个阶段分两步。

### 第一阶段：分析与准备

主要工作：

1. 分析被测端结构
2. 分析集成测试模块
3. 分析集成测试接口
4. 确定集成测试策略
5. 确定测试工具
6. 确定集成测试环境
7. 评估测试工作量

### 第二阶段：测试设计与开发

依据：

* 软件需求
* 概要设计
* 集成测试计划

设计：

* 集成测试用例
* 集成测试规程
* 集成测试代码
* 集成测试脚本

### 阶段输出

1. 集成测试用例
2. 集成测试规程
3. 集成测试代码
4. 集成测试脚本
5. 集成测试设计报告

并通过评审。

---

## 5.3 执行评估阶段

### 开始时间

**单元测试完成后开始集成测试。**

### 依据文档

执行集成测试时依据：

1. 软件需求规格说明书
2. 概要设计
3. 详细设计
4. 代码
5. 单元测试报告

### 主要工作

1. 执行所有集成测试用例
2. 执行回归集成测试用例
3. 记录测试结果
4. 撰写集成测试报告
5. 由测试评估小组或评估人员分析测试结果
6. 输出分析结果

---

## 本节可能出题

### 题1：集成测试过程分为哪三个阶段？

答：计划阶段、设计实现阶段、执行评估阶段。

### 题2：计划阶段的输出是什么？

答：集成测试计划书，并通过评审。

### 题3：执行评估阶段依据哪些资料？

答：软件需求规格说明书、概要设计、详细设计、代码和单元测试报告。

---

# 六、本章总逻辑框架

你可以用这一条主线记住整章：

```text
单元测试完成
↓
多个模块开始组合
↓
重点检查接口、数据传递、模块协作
↓
选择合适集成策略
↓
设计接口层面的测试用例
↓
执行测试、回归测试、评估结果
↓
进入系统测试
```

---

# 七、期末高频考点速记

## 必背定义

**集成测试：在单元测试基础上，将多个模块组合在一起测试，主要检查软件单元之间接口是否正确。**

## 必背区别

**单元测试测模块内部逻辑，集成测试测模块接口和组合功能。**

## 必背任务

1. 数据穿越接口是否丢失
2. 子功能组合是否达到预期
3. 模块之间是否产生不利影响
4. 全局数据结构是否异常
5. 模块误差是否累积放大

## 必背策略

1. 大爆炸集成
2. 自顶向下集成
3. 自底向上集成
4. 三明治集成
5. 核心系统先行集成
6. 持续集成

## 必背辅助模块

**驱动模块 Driver：模拟上级模块。**
**桩模块 Stub：模拟下级模块。**

## 必背过程

**计划阶段 → 设计实现阶段 → 执行评估阶段**

---

# 八、容易出判断题的点

| 判断题                 | 答案        |
| ------------------- | --------- |
| 集成测试可以代替系统测试。       | 错         |
| 未经过单元测试的模块不应进行集成测试。 | 对         |
| 集成测试主要关注模块内部代码逻辑。   | 错         |
| 集成测试主要关注模块接口和模块组合。  | 对         |
| 大爆炸集成适合复杂大型系统。      | 错         |
| 自顶向下集成需要大量桩模块。      | 对         |
| 自底向上集成需要大量驱动模块。     | 对         |
| 自底向上集成可以较早验证高层主控模块。 | 错         |
| 持续集成有助于尽早发现 Bug。    | 对         |
| 集成测试覆盖率主要在代码层面。     | 错，主要在接口层面 |

---

# 九、最可能出的简答题模板

## 1. 简述集成测试的概念和目的

答：

集成测试是在单元测试基础上，将多个模块组合在一起进行测试的过程，主要检查各软件单元之间的接口是否正确。其目的是发现模块之间接口、数据传递、全局数据结构、模块组合功能等方面的问题，是单元测试向系统测试过渡的重要阶段。

---

## 2. 简述集成测试与单元测试的区别

答：

单元测试的对象是单个模块，主要检查模块内部逻辑；集成测试的对象是多个模块及其组合，主要检查模块接口、接口之间的数据传递关系，以及模块组合后是否达到预期功能。单元测试先于集成测试，单元测试质量直接影响集成测试效果。

---

## 3. 比较自顶向下集成和自底向上集成

答：

自顶向下集成从主控模块开始逐步向下集成，需要使用桩模块模拟下级模块，优点是能较早验证主控逻辑，缺点是桩模块开发维护成本高。自底向上集成从底层模块开始逐步向上集成，需要使用驱动模块模拟上级模块，优点是能较早验证底层模块，缺点是高层设计问题发现较晚。

---

## 4. 简述持续集成的作用

答：

持续集成是在软件开发过程中，将已完成的模块尽早、频繁地集成，并通过自动构建、自动部署和自动测试来快速发现问题。它可以避免后期大量 Bug 集中出现，便于定位和修复缺陷，提高软件开发质量和效率。

---

## 5. 简述集成测试过程

答：

集成测试过程分为计划阶段、设计实现阶段和执行评估阶段。计划阶段完成测试计划和策略制定；设计实现阶段建立测试环境，完成测试用例、测试规程、测试代码和脚本设计；执行评估阶段执行测试用例和回归测试，记录结果并形成测试报告。

---

# 十、考前最终背诵版

集成测试是在单元测试基础上，把多个模块组合起来进行测试，主要检查模块之间接口是否正确。它位于单元测试和系统测试之间，是单元测试的扩展和延伸。集成测试重点关注接口、数据传递、模块组合功能、全局数据结构和误差累积问题。集成测试策略包括大爆炸集成、自顶向下集成、自底向上集成、三明治集成、核心系统先行集成和持续集成。自顶向下需要桩模块，自底向上需要驱动模块。集成测试用例主要围绕系统运行、正向测试、逆向测试、特殊需求、高覆盖率和接口依赖关系设计。集成测试过程包括计划阶段、设计实现阶段和执行评估阶段。
